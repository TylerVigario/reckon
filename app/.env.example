# THE LIST OF WHAT THIS APPLICATION READS. Every value below is a production
# default, so nothing here is dangerous if it is copied unchanged and something
# is missed.
#
# For local development, copy it to app/.env and change what differs -- the
# database in particular. It lives beside vite.config.ts because that is the
# only directory Vite reads a .env from; a copy at the repository root is loaded
# by nothing, which looks like configuration and silently is not.
#
# In production nothing reads a .env at all: the service manager supplies these.
# This file is the reference for writing that unit, not the unit itself -- so
# copying it into /etc/sysconfig or the like gets you a starting point, and
# every value still has to be the one this deployment wants.
#
# Not everything here is read by the application. PGHOST and PGDATABASE are also
# read straight from the environment by db/apply.sh and scripts/set-password.mjs
# when they are run from a shell; a .env does not reach those.

# ---------------------------------------------------------------
# Required in production
# ---------------------------------------------------------------

# The public URL a browser used to reach this.
#
# adapter-node compares it against the Origin header to decide whether a form
# submission came from this site. Without it every POST is refused with 403 —
# signing in included, which makes the application look broken rather than
# misconfigured.
#
# Behind a reverse proxy this is the https:// name, NOT the address the process
# listens on.
#
# There is no default that can be right: it names one deployment. example.com
# is a reserved name, so an unchanged copy fails visibly rather than pointing
# somewhere real.
#
# Dev: http://localhost:5173
ORIGIN=https://reckon.example.com

# ---------------------------------------------------------------
# Where it listens
# ---------------------------------------------------------------

# Read by adapter-node itself, not by application code.
#
# HOST unset means 0.0.0.0 — reachable from the network. Where a reverse proxy
# is meant to be the only way in, set 127.0.0.1 so the bind enforces that
# rather than a configuration file claiming it.
HOST=127.0.0.1
PORT=3000

# ---------------------------------------------------------------
# The database
# ---------------------------------------------------------------

# A unix socket by default, which peer-authenticates: the role is decided by the
# operating-system user, so there is no password to leak.
PGHOST=/var/run/postgresql

# The production database. Development uses reckon_dev, and that belongs in a
# developer's own app/.env rather than here -- a file whose default points at a
# development database is one accident away from a production process writing
# invoices into it.
PGDATABASE=reckon

# Wins outright over PGHOST and PGDATABASE when set. For TCP, another machine,
# or a managed service.
# DATABASE_URL=

# ---------------------------------------------------------------
# Optional — addresses
# ---------------------------------------------------------------
#
# TWO keys, because a Google API key carries exactly one application
# restriction. A key can be restricted to HTTP referrers or to IP addresses,
# never both -- so a key the browser uses and a key this server uses cannot be
# the same key without one of them being unrestricted.
#
# Either may be unset on its own. Without the first, addresses are typed rather
# than chosen. Without the second, a chosen address is stored as Google
# returned it and simply is not validated further.

# Google Places API (New). Unset is a supported state: addresses become an
# ordinary text field, which is what they were before this existed.
#
# THE BROWSER KEY. PUBLIC_ is deliberate: it reaches the page, because a
# type-ahead fires on every keystroke and proxying would put a round trip to
# this server in front of each one.
#
#   - Application restriction: HTTP referrers, this deployment's origin
#   - API restriction: Maps JavaScript API, Places API (New)
#
# Both APIs, and the JavaScript one is not optional -- it is what makes the
# referrer restriction work at all. Google applies referrer restrictions to the
# JavaScript API because browsers send the header, while a direct web-service
# call wants an IP restriction, which cannot mean anything when the caller is
# somebody else's browser.
PUBLIC_GOOGLE_MAPS_API_KEY=

# THE SERVER KEY. No PUBLIC_ prefix, so it never reaches a page.
#
#   - Application restriction: IP addresses, this host
#   - API restriction: Address Validation API, Places API (New)
#
# Two APIs, for two jobs it does once per address rather than per keystroke:
#
#   Address Validation -- picking a real place says the place exists; this says
#   the address is deliverable and hands back a corrected form, which is worth
#   having on something a tax district is resolved from.
#
#   Places Details -- the place id comes from the browser, and an assertion from
#   a browser is the one thing a server may not take at face value. This asks
#   Google whether the id is a place, and the answer is what dates the check.
#
# If Places API (New) is left off this key the save still works: a 403 reads as
# "could not confirm", the address is stored, and no verification date is set.
# Unverified is not invalid -- but nothing will ever be dated either, so the
# restriction is worth getting right.
GOOGLE_MAPS_API_KEY=
